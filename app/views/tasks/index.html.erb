<h1>All TASKS</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
      <i class="icon-check"></i> <%= link_to task.title, task_path(task) %>
      <% else%>
      <i class="icon-check-empty"></i> <%= link_to task.title, task_path(task) %>
      <% end %>
    </li>
  <% end %>

</ul>

<h4> <%= link_to "Create a new task", new_path %> </h4>
